import { Pet } from '../entities/Pet.js';

export const pets: Pet[] = [];
export const petIdCounter = { value: 1 };

import { Habit } from '../entities/Pet.js';

export const habits: Habit[] = [];
export const habitCOunter = { value: 1 };
